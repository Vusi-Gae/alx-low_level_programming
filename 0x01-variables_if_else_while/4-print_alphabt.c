#include <stdio.h>
/**
 * main - C program that prints the alphabet in lowercase
 * Return: always 0 (Success)
 */

int main(void)
{
	char x = 'a'

while (x <= 'z')
{
	if (x != q && x != e)
	{
		putchar(x)
	}
	x++
}
	return (0);
}
