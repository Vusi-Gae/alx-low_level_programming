#include <stdio.h>
/**
 * main - C program that prints the alphabet in lowercase
 * Return: always 0 (Success)
 */

int main(void)
{
	char ch = 'a'

while (ch <= 'z')
{
	if (ch != q && ch != e)
	{
		putchar(ch)
	}
	ch++
}
	return (0);
}
