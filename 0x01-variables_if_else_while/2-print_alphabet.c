#include <stdio.h>
/**
 * main - C program that prints the alphabet in lowercase
 * Return: Always 0 (Succes0)
 */

int main(void)
{
	char x;
	x = 'a';

while (x <= 'z')
{
	putchar(z);
	x++;
}
	putchar('\n');
return (0);
}
