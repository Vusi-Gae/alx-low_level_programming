#include <stdio.h>
/**
 * main - This program prints a message using a printf function
 * Return: Always return 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
